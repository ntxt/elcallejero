# The pipeline runs on the standard library plus these.
#   numpy    geometry: projection, lengths, widths, nearest-segment queries
#   pillow   decoding the terrain PNG tiles
#   xlrd     reading INE's legacy .xls name and surname registers
#   openpyxl reading INE's .xlsx municipality dictionary
#
# Also required on PATH: pdftotext (poppler), for the thesis annex extraction.
numpy>=1.26
pillow>=10.0
xlrd>=2.0
openpyxl>=3.1
